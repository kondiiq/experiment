{
  "name": "projects",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^9",
    "express": "^4"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@cap-js/cds-typer": ">=0.1",
    "@cap-js/cds-types": "^0.13.0",
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": ">=8",
    "@sap/eslint-plugin-cds": "^4.1.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.35.0",
    "ts-jest": "^29.4.1",
    "tsx": "^4",
    "typescript": "^5"
  },
  "scripts": {
    "start": "cds-serve",
    "model_types": "npx @cap-js/cds-typer '*' --outputDirectory @cds-models",
    "lint": "npx eslint .",
    "test": "jest",
    "test:mocha": "npx -y mocha"
  },
  "imports": {
    "#cds-models/*": "./@cds-models/*/index.js"
  }
}
